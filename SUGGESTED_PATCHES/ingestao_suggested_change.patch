*** Begin Patch
*** Update File: bronze/ingestao.py
@@
 # ---------------------------------------------------------------------------
 # Autenticação e configurações
 # - As credenciais do GCP são fornecidas via variável de ambiente
 # - O cliente S3 (`boto3`) opera sobre o bucket de destino definido abaixo
 # ---------------------------------------------------------------------------
 GCP_CREDENTIALS_ENV = "GOOGLE_APPLICATION_CREDENTIALS"
@@
 def executar_ingestao_full_bronze():
     # erros_pipeline é local para evitar que múltiplas execuções na mesma sessão acumulem erros
     erros_pipeline = []
-    bq_client = bigquery.Client()
+    # Sugestão: validar antes a existência do caminho informado em
+    # GOOGLE_APPLICATION_CREDENTIALS para falhar cedo. Esta checagem foi propositalmente
+    # deixada como sugestão para que você aprove a alteração antes da aplicação.
+    # Exemplo de função auxiliar (comentada):
+    #
+    # def validar_credenciais_gcp():
+    #     cred_path = os.getenv(GCP_CREDENTIALS_ENV)
+    #     if not cred_path:
+    #         raise RuntimeError(f"Variável {GCP_CREDENTIALS_ENV} não definida. Defina o caminho da chave JSON.")
+    #     if not os.path.isfile(cred_path):
+    #         raise RuntimeError(f"Arquivo de credencial não encontrado em: {cred_path}")
+    #     return cred_path
+    #
+    # Para aplicar a validação, descomente as linhas abaixo:
+    # cred_path = validar_credenciais_gcp()
+    # logger.info(f"Credencial GCP detectada em: {cred_path}")
+    #
+    bq_client = bigquery.Client()
*** End Patch
